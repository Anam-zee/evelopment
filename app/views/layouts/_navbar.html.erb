<nav class='bg-white border-grey'>
  <div class='max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4'>
    <a class='flex items-center space-x-3 rtl:space-x-reverse' onclick='handleLogoClick()' style='cursor: pointer'>
      <img src='/logo.png' class='h-14 w-14' alt='Logo' />
      <span class='self-center text-3xl font-semibold whitespace-nowrap text-grey'>Assessment</span>
    </a>
    <div class='lg:items-center lg:w-auto right-nav' id='navbarSupportedContent'>
      <ul class='flex items-center text-gray-700'>
        <% unless current_user.present? %>
          <li class='mr-4'>
            <%= link_to 'Sign in', new_user_session_path %>
          </li>
          <li class='mr-4'>
            <%= link_to 'Sign up', new_user_registration_path %>
          </li>
        <% else %>
          <li class='mr-4'>
            <button onclick='newEvent(this)'>Create Event</button>
          </li>
          <li class='mr-4'>
            <button onclick='viewAllEvents(this)'>Events</button>
          </li>
          <li>
            <%= button_to 'Sign out', destroy_user_session_path, method: :delete %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
